version: 4
plan_id: fixture_legacy
name: Fixture Legacy V4
run:
  pre_orchestrator:
    enabled: false
  post_orchestrator:
    enabled: true
agents:
  - id: worker_default
    role: worker
    provider: openai
    model: gpt-4.1-mini
tasks:
  - id: t1
    title: Legacy task
    completed: false
outputs:
  required_artifacts: []
